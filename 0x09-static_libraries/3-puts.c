#include "main.h"
void _puts(char *str)
#include<stdio.h>
/**
 * main - Entry point
 *_Puts - takes  string as an input
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
